# Core
numpy>=1.26
pandas>=2.1
scipy>=1.11
pyarrow>=14.0          # parquet IO for data/processed

# Data acquisition
yfinance>=0.2.40

# Statistics / econometrics
statsmodels>=0.14
pmdarima>=2.0
arch>=6.3

# Machine learning
scikit-learn>=1.3
xgboost>=2.0
optuna>=3.5
shap>=0.44              # optional: SHAP importance for XGBoost (gracefully skipped if missing/incompatible)

# Deep learning (optional, heavy - LSTM/GRU only; the rest of the pipeline
# runs fully without it - delete this line to skip it)
tensorflow>=2.15

# Optional secondary benchmark (Task 3.2)
prophet>=1.1             # optional; skipped with a warning if not installed

# Plotting
matplotlib>=3.8
seaborn>=0.13

# Notebooks
jupyter>=1.0
nbformat>=5.10

# Dev / testing
pytest>=8.0
tabulate>=0.9
